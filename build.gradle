buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.5.0' // Versão atual do plugin
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
